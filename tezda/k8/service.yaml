apiVersion: v1
kind: Service
metadata:
  name: redis-test-server-service
spec:
  selector:
    app: redis-test-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
